{
  "name": "diehard-serverless-demo",
  "version": "1.0.0",
  "repository": "https://github.com/jcummins54/diehard",
  "author": "Jeremy Cummins",
  "license": "MIT",
  "scripts": {
    "start:front": "cd front && yarn start",
    "start:api": "cd serverless && yarn start",
    "start": "npm-run-all --parallel start:front start:api",
    "lint:front": "cd front && yarn lint",
    "lint:api": "cd serverless && yarn lint",
    "lint": "npm-run-all lint:front lint:api"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
